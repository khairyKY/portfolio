---
const projects = [
  { title: 'Shaheen E-Commerce', description: 'Solo-rescued a live Laravel 12 e-commerce platform — 300+ commits, 100+ PRs merged, 55+ production issues closed.', tags: ['Full-Stack', 'Backend', 'Infrastructure'], tech: ['Laravel 12', 'Livewire 3', 'Filament 4', 'GitHub Actions'], slug: 'shaheen-ecommerce' },
  { title: 'Sotto', description: 'Offline desktop voice dictation — Rust/Tauri with Parakeet ASR (ONNX, Vulkan GPU), llama.cpp AI polish, and Harper grammar engine.', tags: ['Full-Stack', 'AI/ML'], tech: ['Rust', 'Tauri v2', 'Parakeet ASR', 'llama.cpp'], slug: 'sotto' },
  { title: 'Sukoon', description: 'Android CGM glucose viewer for Type 1 diabetes — Jetpack Compose, interactive time-ranged graphs, built from personal medical need.', tags: ['Frontend', 'UI/UX'], tech: ['Kotlin', 'Jetpack Compose', 'Android'], slug: 'sukoon' },
  { title: "Kai's Flow", description: 'Productivity PWA — calendar time-blocking, task management, journal, Pomodoro timer. React 19 + Supabase with a botanical design system.', tags: ['Full-Stack', 'Frontend'], tech: ['React 19', 'TypeScript', 'Supabase', 'FullCalendar'], slug: 'kais-flow' },
  { title: 'Customer Ordering System', description: 'Full-stack vertical-slice e-commerce — React frontend, FastAPI backend, security middleware, Playwright E2E. My project with team contributors.', tags: ['Full-Stack', 'Frontend', 'Backend'], tech: ['React 18', 'FastAPI', 'Tailwind', 'Playwright'], slug: 'customer-ordering-system' },
  { title: 'Scentura → Aeron ERP', description: 'Contributed the Aeron frontend prototype (23 React route modules) for a perfumery ERP system.', tags: ['Full-Stack', 'Frontend', 'UI/UX'], tech: ['React 19', 'TypeScript', 'shadcn/ui', 'Framer Motion'], slug: 'scentura-erp' },
  { title: 'MRL Eye Classification', description: 'DenseNet121 + ViT drowsiness detector — contributed model training with real-time webcam inference and Arduino buzzer alert.', tags: ['AI/ML'], tech: ['PyTorch', 'OpenCV', 'CUDA', 'DenseNet'], slug: 'mrl-eye-classification' },
  { title: 'Bellissimo Coffee Shop', description: 'Liquid Glass design system with circadian lighting engine and animated BaristaButton. Entirely my own project.', tags: ['Frontend', 'UI/UX'], tech: ['React 19', 'Vite', 'Tailwind', 'Framer Motion'], slug: 'bellissimo-coffee-shop' },
  { title: 'ErgoX-UI', description: 'SwiftUI gaze-controlled interface using ARKit TrueDepth — contributed UI components, messaging views, and media player logic.', tags: ['Frontend', 'UI/UX'], tech: ['SwiftUI', 'ARKit', 'Swift', 'iPhone'], slug: 'ergox-ui' },
  { title: 'Study Guides', description: 'Community study resources for Embedded Systems, Algorithms, and Computer Networks — built as offline-first HTML with interactive features.', tags: ['Frontend'], tech: ['HTML/CSS', 'JavaScript', 'Python'], slug: 'networks-study-guide' },
  { title: 'Hospital Management System', description: 'Python desktop app with Flet GUI, SQLite database, and role-based dashboards — contributed auth, database layer, and admin panels.', tags: ['Backend'], tech: ['Python', 'Flet', 'SQLite'], slug: 'hospital-management-system' },
  { title: 'Arduino Robotics', description: '3 autonomous subsystems: maze solver, light seeker, and Bluetooth arena robot with environmental sensing.', tags: ['Embedded'], tech: ['Arduino C++', 'RemoteXY', 'HC-SR04'], slug: 'arduino-robotics' }
];

const filters = ['All', 'Full-Stack', 'Frontend', 'UI/UX', 'Backend', 'AI/ML', 'Embedded'];
const filterColors = ['#ED6F5C', '#A78BFA', '#00BFFF', '#4ECDC4', '#E9B94A', '#34D399', '#ED6F5C'];
---

<section id="projects">
  <div class="container">
    <div class="section-header" data-reveal>
      <div class="label">Projects</div>
      <h2 class="display">
        What I've <em>built</em><span class="dot">.</span>
      </h2>
    </div>

    <div class="pill-bar" data-reveal id="filter-bar">
      {filters.map((f, i) => (
        <button class="pill" data-filter={f === 'All' ? 'all' : f} data-color={filterColors[i]}>
          {f}
        </button>
      ))}
    </div>

    <div class="project-grid" id="project-grid">
      {projects.map((p, i) => (
        <article class="card" data-reveal data-tags={p.tags.join(',')}${i === 0 ? ' data-featured' : ''}>
          <a href={`/project/${p.slug}`}>
            <div class="num">
              <span>{String.fromCharCode(0x2170 + i)}.</span>
              {p.tags.length > 0 && <span class="tag">{p.tags[0]}</span>}
            </div>
            <h3>{p.title}</h3>
            <p>{p.description}</p>
            <div class="card-tech">
              {p.tech.slice(0, 4).map(t => <span class="tech-pill">{t}</span>)}
            </div>
            <div class="arrow-mark">
              <svg width="14" height="14" viewBox="0 0 16 16"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" fill="none" stroke-width="1.6"/></svg>
            </div>
          </a>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .pill-bar {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 40px;
  }
  .project-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
  .card a {
    text-decoration: none;
    color: inherit;
    display: block;
  }
  .card-tech {
    display: flex;
    flex-wrap: wrap;
    gap: 5px;
    margin-top: 14px;
  }
  .card { padding-bottom: 32px; }
  .card[data-featured] { grid-column: span 2; }
  .card[data-featured] a { display: flex; flex-direction: column; height: 100%; }
  .card[data-featured] h3 { font-size: 20px; }
  .card[data-featured] .num span:first-child { font-size: 20px; }
  .card[data-hidden] { display: none; }
  @media (max-width: 1080px) {
    .project-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 640px) {
    .project-grid { grid-template-columns: 1fr; }
  }
</style>

<script>
  const filterBar = document.getElementById('filter-bar');
  const grid = document.getElementById('project-grid');
  const cards = grid?.querySelectorAll('.card');

  filterBar?.addEventListener('click', (e) => {
    const btn = e.target.closest('.pill');
    if (!btn) return;
    filterBar.querySelectorAll('.pill').forEach(p => p.classList.remove('active'));
    btn.classList.add('active');
    const filter = btn.dataset.filter;
    cards?.forEach(card => {
      if (filter === 'all') {
        card.removeAttribute('data-hidden');
      } else {
        const tags = (card.dataset.tags || '').split(',');
        card.toggleAttribute('data-hidden', !tags.includes(filter));
      }
    });
  });

  filterBar?.querySelector('.pill')?.classList.add('active');
</script>
